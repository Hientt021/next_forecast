import StoreProvider from "@/src/lib/redux/StoreProvider";
import type { Metadata } from "next";
import dynamic from "next/dynamic";
import { Roboto } from "next/font/google";
import React from "react";
import "./globals.css";

const roboto = Roboto({ weight: "400", subsets: ["latin"] });
const Dimension = dynamic(() => import("@/src/hook/useDevice"), {
  ssr: false,
});

const SnackProvider = dynamic(
  () => import("@/src/lib/notistack/SnackbarProvider"),
  {
    ssr: false,
  }
);

const MuiThemeProvider = dynamic(() => import("@/src/lib/mui/ThemeProvider"), {
  ssr: false,
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={roboto.className} style={{ position: "relative" }}>
        <MuiThemeProvider>
          <StoreProvider>
            <SnackProvider>
              <Dimension />
              {children}
            </SnackProvider>
          </StoreProvider>
        </MuiThemeProvider>
      </body>
    </html>
  );
}
